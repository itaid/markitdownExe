# 转换核心（锁版本，升级需验证转换结果一致性）
markitdown[pdf,docx,pptx,xlsx]==0.1.7

# 本地 OCR（扫描件/纯图片）
# numpy 必须 <2（rapidocr/torch 生态兼容），opencv 固定 4.x
rapidocr_onnxruntime>=1.3,<2
pypdfium2>=4.30
Pillow>=10.0
opencv-python-headless==4.10.0.84
numpy>=1.24,<2

# GUI
PySide6>=6.6,<7

# 后处理
lxml>=5.0

# 测试（构造 docx/pptx 夹具）
python-docx>=1.1
python-pptx>=0.6
